name: "Test"
on:
  pull_request:
  push:
jobs:
  checks:
    runs-on: native
    strategy:
      matrix:
        # check: [pre-commit-check, nixos-tests]
        check: [pre-commit-check]
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - uses: ./.github/workflows/setup-env.yml
      secrets: inherit
    - name: Build
      run: nix build --print-build-logs .#checks.x86_64-linux.${{ matrix.check }}
  nextest:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: ./.github/workflows/setup-env.yml
      secrets: inherit
    - name: Nextest
      run: nix develop --print-build-logs -v --command cargo nextest run
